apiVersion: apps/v1
kind: Deployment
metadata:
  name: istioapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: istioapp
  template:
    metadata:
      labels:
        app: istioapp
    spec:
      containers:
      - name: istioapp
        image: wandpsilva/istioapp:latest
        ports:
        - containerPort: 80
